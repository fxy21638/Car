# 智能小车固件工程审查报告

## 1. 报告信息

| 项目 | 内容 |
| --- | --- |
| 工程目录 | `C:\Users\YunaCelisse\Desktop\Software\Car` |
| 审查日期 | 2026-07-10 |
| 目标平台 | TI MSPM0G3507 / Cortex-M0+ |
| 当前主要工程 | Keil µVision 工程 |
| 审查方式 | 静态代码与工程配置审查 |
| 代码状态 | Git 工作区在审查时无未提交改动 |

## 2. 审查结论

工程已经具备循迹、编码器测速、双环 PID、超声波避障、MPU6050 航向估计、OLED 菜单和多任务状态机等完整功能框架，代码中也采用了非阻塞 OLED 刷新、UART 环形缓冲区、PID 输出限幅及积分抗饱和等合理设计。

但当前版本仍存在会直接影响车辆安全性和运行稳定性的缺陷，暂不建议在无人看护或高速状态下运行。最优先的问题是：运行期间急停按键未被扫描、超声波无回波时测距永久锁死、PID 执行周期不固定、IMU 故障时仍可能持续驱动电机。

综合评价：**具备课程设计和实验验证基础，但在实时性、故障保护、构建一致性和可验证性方面仍需整改。**

## 3. 审查范围与边界

本次重点检查了以下内容：

- 主程序初始化与超级循环；
- 电机、编码器、循迹传感器、超声波、MPU6050、OLED、UART 驱动；
- PID 控制和避障、对角线任务状态机；
- 中断与主循环之间的共享状态；
- Keil、GCC、TIClang 构建配置；
- 故障处理、看门狗、测试和源码组织。

本次未获得原理图、电机驱动器完整型号与接线定义，也未连接实车、示波器或逻辑分析仪；当前环境未检测到对应交叉编译工具链。因此，下述结论以源码静态证据为基础，PWM 极性、电气安全、真实控制周期和抗干扰能力仍需结合原理图及实测确认。

## 4. 风险分级

| 等级 | 定义 |
| --- | --- |
| 严重 | 可能导致急停失效、持续驱动、失控或核心功能永久失效，应立即修复 |
| 高 | 明显影响控制稳定性、故障恢复或构建正确性，应在下一次上车前修复 |
| 中 | 可能引发偶发误差、性能下降或维护风险，建议纳入近期整改 |
| 低 | 健壮性、规范性和长期维护方面的改进项 |

## 5. 详细发现

### CAR-001：运行期间急停按键无法生效

- 风险等级：**严重**
- 证据位置：`empty.c:84-87`、`empty.c:242-248`
- 现象：主循环只在 `g_running == 0` 时调用 `Handle_Keys()`，而 K1 急停逻辑位于该函数内部。车辆进入运行状态后，所有按键都停止扫描。
- 影响：OLED 虽显示 `K1:Stop`，但运行期间按下 K1 不会执行 `Set_PWM(0, 0)`。
- 建议：将急停输入放在主循环最高优先级路径中持续扫描；急停时关闭 PWM、清除运行标志、重置 PID 和任务状态。若硬件允许，应增加独立 GPIO 中断或直接切断电机使能的硬件急停路径。
- 验证：分别在循迹、避障和转向状态下按下 K1，测量从按键有效到 PWM 关闭的最坏延迟。

### CAR-002：超声波无回波时测距状态永久锁死

- 风险等级：**严重**
- 证据位置：`keil/Hardware/Ultrasonic.c:24-26`、`:39-63`、`:94-105`
- 现象：`s_echoUs` 仅在 Echo 为高电平时递增，超时判断同样依赖 `s_echoUs`。若 Echo 始终不拉高，`s_echoUs` 永远为零，`s_busy` 永远不会清除。已经声明的 `s_waitUs` 没有参与计时。
- 影响：传感器断线、目标超量程或干扰导致无上升沿后，系统不会再次触发测距；避障功能长期失效。
- 建议：分别维护总等待时间和 Echo 高电平时间；无论 Echo 电平如何，总等待时间都应递增并触发超时。更推荐使用 GPIO 双边沿中断配合定时器捕获。
- 验证：断开 Echo、保持 Echo 低、保持 Echo 高并重复测试，确认每次均能在规定时间内退出测量并重新触发。

### CAR-003：超声波定时器产生持续的高频无效中断

- 风险等级：**高**
- 证据位置：`keil/Hardware/Ultrasonic.c:29-34`、`:86-90`
- 现象：50 µs 周期定时器初始化后持续运行，即使 `s_busy == 0` 也产生中断，频率约为 20 kHz。
- 影响：占用 CPU，增加编码器、SysTick 和控制循环的中断延迟及抖动。
- 建议：仅在测距窗口启动定时器，测量完成或超时后停止；或者改用输入捕获，由硬件记录脉宽。
- 验证：使用 GPIO 翻转或性能计数统计 ISR 占用率，对比整改前后的控制周期抖动。

### CAR-004：PID 控制循环没有固定周期

- 风险等级：**严重**
- 证据位置：`empty.c:67-132`、`keil/Hardware/Encoder.c:61-75`、`keil/Hardware/PID.c:43-82`
- 现象：编码器速度每 10 ms 更新一次，但 `PID_control()`、`PID_control_head()` 和 `TurnToAngle()` 随超级循环反复执行。PID 算法未使用时间步长 `dt`，积分和微分均按“调用次数”计算。
- 影响：同一速度反馈可能被重复积分多次；积分很快到达限幅，微分项只在反馈改变的第一次调用有效。实际增益会随 OLED、软件 I²C、UART 等负载变化，导致参数不可重复、控制抖动甚至失稳。
- 建议：由 10 ms 定时器 ISR 只发布控制事件，主循环每个事件执行一次“采样—计算—输出”；PID 参数明确绑定 `dt = 10 ms`，或者将积分、微分公式显式加入 `dt`。
- 验证：在控制循环入口翻转调试 GPIO，使用逻辑分析仪确认周期和最坏抖动；建议目标抖动小于周期的 1%。

### CAR-005：IMU 初始化或运行故障时没有进入安全状态

- 风险等级：**严重**
- 证据位置：`empty.c:77-82`、`:263-272`、`:349-362`，`keil/Hardware/task_v2.c:78-119`、`:225-288`
- 现象：MPU6050 初始化失败只设置 `gMPU6050_OK = 0`，但仍可启动避障和对角线任务；主循环忽略 `MPU6050_UpdateYaw()` 返回值。转向状态没有超时保护。
- 影响：航向值失效或停留在旧值时，车辆可能持续执行原地转向或错误的航向控制。
- 建议：IMU 不可用时禁止启动相关任务；运行时统计连续读取失败次数，超过阈值立即停车并进入故障状态；所有转向状态增加时间与编码器距离双重超时。
- 验证：启动前拔掉 MPU6050、运行中断开 SDA/SCL、注入 NACK，确认系统均能停止电机并报告故障。

### CAR-006：编码器累计值读取和复位存在竞争窗口

- 风险等级：**中**
- 证据位置：`keil/Hardware/Encoder.c:37-55`、`:81-146`
- 现象：GPIO ISR 更新左右轮累计计数时，主循环可能同时读取或清零这些变量。单个 32 位访问虽通常是原子的，但双轮数据并非一致快照，复位过程也可能丢失脉冲。
- 影响：圈数、阶段距离和停止条件可能出现偶发偏差。
- 建议：使用短临界区完成双轮快照和复位；临界区只包含必要的复制或清零操作。
- 验证：在高速编码器输入下反复复位并记录总脉冲，与外部计数器结果对比。

### CAR-007：MPU 更新标志可能丢事件

- 风险等级：**中**
- 证据位置：`empty.c:77-82`、`keil/Hardware/Encoder.c:74`
- 现象：主循环完成 I²C 读取后才清除 `g_mpu6050_flag`。若 TIMA0 ISR 在读取期间再次置位，随后执行的清零会覆盖新事件。
- 影响：产生 10/20 ms 混合采样间隔，增加航向估计和控制抖动。
- 建议：在开始处理前清除标志，或者使用饱和事件计数器；积分仍应使用实际时间差。
- 验证：记录连续更新的时间戳，确认不存在非预期的事件丢失。

### CAR-008：OLED 持续满速刷新并占用共享软件 I²C

- 风险等级：**中**
- 证据位置：`empty.c:121-125`、`keil/Hardware/OLED.c:641-676`、`keil/Hardware/MPU6050_MSPM0.c:30-33`
- 现象：OLED 完成一次刷新后，主循环立即清屏、重绘并启动下一次刷新。OLED 与 MPU6050 共用软件 I²C 引脚。
- 影响：持续占用 CPU 和总线，使控制循环执行时间随显示内容变化，并增加软件 I²C 受电机干扰的概率。
- 建议：显示刷新限制为 5～10 Hz，或仅在数据变化时更新；保留脏区域机制，避免每次清空完整帧缓冲。条件允许时改用硬件 I²C。
- 验证：比较关闭显示、限频显示和当前显示模式下的控制周期抖动及 IMU 读失败率。

### CAR-009：GCC 和 TIClang 构建配置不完整

- 风险等级：**高**
- 证据位置：`gcc/makefile:15-36`、`:73-79`，`ticlang/makefile` 对应规则
- 现象：Makefile 只编译 `empty.c` 和 SysConfig 生成文件，没有包含任何电机、PID、传感器或任务实现；头文件搜索路径也未包含有效的 Hardware 目录。
- 影响：非 Keil 构建会在头文件解析或链接阶段失败，README 所表达的多工具链工程能力与实际状态不一致。
- 建议：建立唯一源码清单，由 Keil、GCC、TIClang/IAR 共用；在 CI 中至少执行一次 GCC 或 TIClang 编译。
- 验证：从干净目录执行完整构建，确认无缺失符号、无警告，并检查生成的 map 文件。

### CAR-010：存在两套相互分叉的 Hardware 源码

- 风险等级：**高**
- 证据位置：`Hardware/`、`keil/Hardware/`
- 现象：两个目录包含同名驱动，但内容差异明显；Keil 当前引用 `keil/Hardware`，其他工程配置与根目录源码关系不清晰。
- 影响：维护人员可能修改未参与构建的文件；不同工具链可能产生不同行为。
- 建议：保留唯一的 `src/drivers` 或 `Hardware` 目录，工具链目录只保存工程描述、启动文件和链接脚本。
- 验证：对所有构建配置输出实际参与编译的源文件列表，确认一致。

### CAR-011：OLED 格式化接口存在缓冲区溢出风险

- 风险等级：**中**
- 证据位置：`keil/Hardware/OLED.c:1132-1139`
- 现象：`OLED_Printf()` 使用 30 字节局部数组和无长度限制的 `vsprintf()`。
- 影响：较长格式化结果会覆盖栈，可能导致随机故障或 HardFault。
- 建议：改为 `vsnprintf(String, sizeof(String), format, arg)`，并将 `format` 参数声明为 `const char *`。
- 验证：传入超过 30 字节的字符串，确认输出被安全截断且栈未损坏。

### CAR-012：故障处理、看门狗和自动化测试不足

- 风险等级：**高**
- 证据位置：`keil/startup_mspm0g350x_uvision.s:145-170`；工程中未发现测试目录或看门狗使用代码
- 现象：HardFault 和默认异常处理器仅无限循环；没有看到独立看门狗、复位原因记录、关键传感器自检或自动化测试。
- 影响：异常后可能长期保持最后一次电机输出；回归缺陷依赖实车人工发现。
- 建议：故障入口首先将电机置于硬件安全状态，再保存复位原因并由看门狗复位；为纯算法和状态机增加主机单元测试。
- 验证：人为触发 HardFault 和主循环卡死，确认电机立即停止且设备可恢复启动。

## 6. 值得保留的设计

以下实现方向合理，整改时建议保留：

- PID 已包含输出限幅、积分限幅和条件积分抗饱和；
- 避障与转向使用显式状态机，避免单个长时间阻塞函数；
- OLED 具备脏页和分步刷新机制；
- UART 采用静态环形缓冲区，无动态内存分配；
- 编码器分别保存速度窗口计数和累计距离计数；
- 使用无符号减法比较毫秒时间，具备基础的计数回绕兼容性；
- MPU6050 读取包含 ACK 检查、尖峰抑制和基于实际时间差的积分。

## 7. 建议整改顺序

### 第一阶段：上车前必须完成

1. 修复运行期间急停；
2. 修复超声波无回波锁死；
3. 将控制循环固定为 10 ms；
4. 增加 IMU 故障停车与转向超时；
5. 验证 `Set_PWM(0, 0)` 在实际电机驱动器上的安全电平。

### 第二阶段：稳定性整改

1. 超声波改为边沿中断或输入捕获；
2. 为编码器快照和复位增加临界区；
3. OLED 刷新限频；
4. 加入看门狗、故障安全处理和复位原因记录；
5. 检查并明确所有中断优先级。

### 第三阶段：工程化整改

1. 合并两套 Hardware 源码；
2. 修复 GCC/TIClang 构建；
3. 将任务参数、速度、距离和角度阈值集中到配置头文件；
4. 建立单元测试与持续构建；
5. 补充 README 中的接线、构建、烧录、标定和故障处理说明。

## 8. 建议验证清单

### 软件与构建

- [ ] Keil 全量重编译无错误、无新增警告；
- [ ] GCC 或 TIClang 可从干净目录独立构建；
- [ ] 检查 map 文件中的 Flash、RAM 和栈余量；
- [ ] PID、角度回绕、状态机和超时逻辑具有单元测试；
- [ ] 所有外部输入均有范围或超时检查。

### 实时性

- [ ] 控制周期固定为 10 ms；
- [ ] 逻辑分析仪测得控制周期最坏抖动小于 1%；
- [ ] ISR 中没有 I²C、OLED、格式化输出或长时间忙等；
- [ ] 超声波空闲时不产生 20 kHz 周期中断。

### 故障与安全

- [ ] 任意运行状态下急停都能可靠关闭电机；
- [ ] Echo 断线、恒高、恒低均能超时恢复；
- [ ] MPU6050 启动失败或运行断线时立即停车；
- [ ] 编码器断线或电机堵转可被检测；
- [ ] HardFault、看门狗复位和欠压复位后输出保持安全状态；
- [ ] 完成至少 1000 次上电/掉电循环测试。

### 实车与硬件

- [ ] 用示波器确认 PWM 频率、极性、零输出和方向切换波形；
- [ ] 确认 H 桥换向没有直通风险；
- [ ] 测量电机启动、堵转时电源跌落和 MCU 复位情况；
- [ ] 测量静止状态 MPU6050 噪声、零偏和温漂；
- [ ] 在电机满负载时统计 I²C 读取失败率；
- [ ] 对编码器、超声波和循迹输入进行断线故障注入。

## 9. 最终建议

当前最适合继续采用裸机事件驱动架构，无需为了整改而引入 RTOS。优先把控制环、显示、通信和传感器任务改造成明确周期的协作式调度，并为每个依赖物理反馈的状态增加超时和安全退出。完成第一阶段整改及故障注入验证后，再进行 PID 参数重新标定；否则当前参数会继续受到不固定调用频率影响，标定结果缺乏可重复性。

## 10. 整改跟踪（2026-07-10）

| 编号 | 软件整改状态 | 仍需验证 |
| --- | --- | --- |
| CAR-001 | 已实现全状态按键扫描和统一急停 | 实车测量急停最坏延迟 |
| CAR-002/003 | 已增加总超时、Echo 状态并仅在测量期间启动定时器 | Echo 断线、恒高、恒低故障注入 |
| CAR-004 | 已改为 TIMA0 发布的固定 10 ms 控制事件 | 逻辑分析仪测量周期抖动、重新确认 PID |
| CAR-005 | 已增加启动门禁、连续失败和状态超时停车 | 运行中拔除 MPU6050 |
| CAR-006/007 | 已增加编码器一致快照并取消 MPU 二值事件标志 | 高速编码器输入压力测试 |
| CAR-008 | 已将 OLED 刷新限制为 100 ms | 比较整改前后 I2C 失败率 |
| CAR-009/010 | 已统一 `src` 源码并提供本地 SDK 的 Keil/GCC 配置 | 本机 Keil、Arm GCC 实际构建 |
| CAR-011 | 已使用 `vsnprintf`，并修复 UART `INT_MIN` | 长字符串测试 |
| CAR-012 | 已增加 WWDT、异常安全停车、算法测试骨架 | 看门狗及 HardFault 实机复位测试 |

TI Arm Clang 已完成全量编译和链接验证：代码约 21.4 KB，BSS 约 2.6 KB。硬件验收完成前，上述项目不能视为最终关闭。
