# MSPM0G3507 智能小车技术文档

## 1. 工程概述

本工程是一个基于 `TI MSPM0G3507` 的两轮智能小车控制项目，包含以下能力模块：

- 电机 PWM 调速与方向控制
- 八路循迹传感器输入
- 双路编码器测速/计数
- OLED 菜单与状态显示
- MPU6050 偏航角读取
- 超声波测距
- 按键菜单控制
- 串口数据输出
- PID 闭环控制

工程同时保留了 `Keil`、`IAR`、`GCC`、`ticlang` 多套工程文件，其中当前最完整、路径最匹配的是 `Keil` 工程。

## 2. 目录结构

- `App/`
  - `empty.c`：主程序入口、系统初始化、主循环、菜单逻辑
  - `task.c`：避障状态机、转向状态机
- `Bsp/`
  - `Motor.*`：电机驱动
  - `Sensor.*`：循迹传感器
  - `Encoder.*`：编码器输入与计数
  - `OLED.*` / `OLED_Data.*`：OLED 显示
  - `MPU6050_MSPM0.*`：软件 I2C 驱动 MPU6050
  - `Ultrasonic.*`：超声波测距
  - `Key.*`：按键扫描
  - `Uart.*`：串口输出、VOFA 数据发送
  - `Delay.*`：延时与节拍支持
- `System/`
  - `PID.*`：PID 控制算法
- `empty.syscfg`
  - SysConfig 图形化硬件配置源文件
- `ti_msp_dl_config.c/.h`
  - 由 SysConfig 自动生成的底层初始化代码
- `keil/` `iar/` `gcc/` `ticlang/`
  - 不同编译工具链的工程文件

## 3. 当前软件状态

### 3.1 初始化状态

`System_Init()` 当前会初始化以下模块：

- SysTick
- 电机
- 循迹传感器
- GPIOA 中断、TIMA0 定时器中断
- 编码器
- 按键
- 超声波
- OLED
- MPU6050
- PID 参数

### 3.2 主循环状态

当前 `App/empty.c` 中的大部分完整运行逻辑被注释掉了，主循环实际执行的是：

- `VOFA_SendSpeedLoop();`
- `Set_PWM(50, 50);`

这意味着当前固件上电后更接近“固定占空比驱动电机并输出调试数据”的状态，而不是完整启用：

- 循迹闭环
- 圈数控制
- 避障逻辑
- IMU 转向逻辑
- OLED 菜单运行逻辑

### 3.3 功能状态汇总

| 模块 | 代码存在 | 已初始化 | 当前主循环实际使用 |
| --- | --- | --- | --- |
| 电机 | 是 | 是 | 是 |
| 串口/VOFA | 是 | 依赖 SysConfig | 是 |
| 循迹传感器 | 是 | 是 | 否 |
| 编码器 | 是 | 是 | 间接依赖，当前未闭环使用 |
| OLED | 是 | 是 | 否 |
| 按键 | 是 | 是 | 否 |
| 超声波 | 是 | 是 | 否 |
| MPU6050 | 是 | 是 | 否 |
| PID 控制 | 是 | 是 | 否 |
| 避障状态机 | 是 | 否 | 否 |
| 转向状态机 | 是 | 否 | 否 |

## 4. 接线状态总表

以下接线信息以 `empty.syscfg` 和 `ti_msp_dl_config.h` 的当前配置为准。

### 4.1 核心控制与调试接口

| 功能 | 信号 | 芯片引脚 | 说明 | 状态 |
| --- | --- | --- | --- | --- |
| SWD 调试 | SWDIO | PA19 | 下载/调试 | 已配置 |
| SWD 调试 | SWCLK | PA20 | 下载/调试 | 已配置 |
| 串口 | TX | PA10 | UART0_TX，115200 | 已配置 |
| 串口 | RX | PA11 | UART0_RX，115200 | 已配置 |
| 板载 LED | LED | PA2 | SysConfig 已建议引脚 | 已建议，代码中未见主要使用 |

### 4.2 电机驱动接线

| 模块 | 信号 | 芯片引脚 | 说明 | 状态 |
| --- | --- | --- | --- | --- |
| 左电机方向 | AIN1 | PA24 | 左轮方向控制 1 | 已配置 |
| 左电机方向 | AIN2 | PA16 | 左轮方向控制 2 | 已配置 |
| 右电机方向 | BIN1 | PB6 | 右轮方向控制 1 | 已配置 |
| 右电机方向 | BIN2 | PB7 | 右轮方向控制 2 | 已配置 |
| 左/右电机 PWM | PWM0 CH0 | PB20 | `TIMG12_CCP0` | 已配置 |
| 左/右电机 PWM | PWM0 CH1 | PA25 | `TIMG12_CCP1` | 已配置 |

说明：

- 方向引脚由 `MOTOR` GPIO 组控制。
- 调速引脚由 `PWM_0` 提供两路 PWM 输出。
- 具体左右轮与两个 PWM 通道的一一对应关系，建议结合 `Bsp/Motor.c` 再核对一次实际接线。

### 4.3 循迹传感器接线

`TRACK_SENSOR` 为 8 路输入，全部位于 `PORTB`。

| 传感器 | 芯片引脚 | 状态 |
| --- | --- | --- |
| S0 | PB24 | 已配置 |
| S1 | PB14 | 已配置 |
| S2 | PB19 | 已配置 |
| S3 | PB18 | 已配置 |
| S4 | PB17 | 已配置 |
| S5 | PB16 | 已配置 |
| S6 | PB2 | 已配置 |
| S7 | PB9 | 已配置 |

说明：

- 当前配置为数字输入。
- 主循环中的循迹位置读取 `Sensor_GetQuantizedPos()` 已存在，但目前被注释。

### 4.4 编码器接线

两路编码器都接在 `GPIOA`，并启用了上升沿中断。

| 编码器 | 信号 | 芯片引脚 | 配置 | 状态 |
| --- | --- | --- | --- | --- |
| 编码器 A | E1A | PA15 | 上拉输入 + 上升沿中断 | 已配置 |
| 编码器 A | E1B | PA14 | 上拉输入 + 上升沿中断 | 已配置 |
| 编码器 B | E2A | PA13 | 上拉输入 + 上升沿中断 | 已配置 |
| 编码器 B | E2B | PA12 | 上拉输入 + 上升沿中断 | 已配置 |

说明：

- 编码器用于速度反馈和圈数统计。
- 代码中已包含 `Get_Current_Circles()` 等接口。

### 4.5 OLED 接线

| 模块 | 信号 | 芯片引脚 | 说明 | 状态 |
| --- | --- | --- | --- | --- |
| OLED | SDA | PA0 | 软件时序接口 | 已配置 |
| OLED | SCL | PA1 | 软件时序接口 | 已配置 |

说明：

- 这里不是硬件 I2C 外设映射，而是普通 GPIO 方式驱动。
- OLED 初始化已执行，但当前主循环未刷新菜单。

### 4.6 MPU6050 接线

| 模块 | 信号 | 芯片引脚 | 说明 | 状态 |
| --- | --- | --- | --- | --- |
| MPU6050 | SDA | PA7 | 软件 I2C，内部上拉 | 已配置 |
| MPU6050 | SCL | PA6 | 软件 I2C，内部上拉 | 已配置 |

说明：

- `MPU6050_Init()` 和陀螺仪 Z 轴校准在启动阶段会执行。
- 若初始化失败，程序内部会将 `gMPU6050_OK` 置 0。

### 4.7 超声波接线

| 模块 | 信号 | 芯片引脚 | 说明 | 状态 |
| --- | --- | --- | --- | --- |
| 超声波 | Trig | PB8 | 触发输出 | 已配置 |
| 超声波 | Echo | PB15 | 回响输入 | 已配置 |
| 捕获输入 | CAPTURE0 | PA17 | `TIMA1_CCP0` | 已配置 |

说明：

- 超声波测距除了 `Trig/Echo` 外，还配置了 `CAPTURE_0` 用于计时/捕获。
- 避障逻辑代码存在，但当前主循环没有启用。

### 4.8 独立按键接线

| 按键 | 芯片引脚 | 用途 | 状态 |
| --- | --- | --- | --- |
| KEY0 | PA22 | 菜单返回/急停 | 已配置 |
| KEY1 | PA27 | 菜单加/选择 | 已配置 |
| KEY2 | PA28 | 菜单减/选择 | 已配置 |
| KEY3 | PA31 | 菜单确认/启动 | 已配置 |

说明：

- 按键接口已初始化。
- 菜单状态机在代码中保留，但当前主循环未调用 `Handle_Keys()`。

## 5. 时钟与外设配置

当前 SysConfig 生成配置显示：

- `CPUCLK_FREQ = 80 MHz`
- `UART0 = 115200`
- `PWM_0` 使用 `TIMG12`
- `TIMER_0` 使用 `TIMA0`
- `TIMER_US` 使用 `TIMG0`
- `CAPTURE_0` 使用 `TIMA1`

## 6. 软件控制流程

设计上，工程 intended 的运行流程如下：

1. `SYSCFG_DL_init()` 完成底层初始化
2. `System_Init()` 初始化各硬件模块
3. 读取循迹传感器位置
4. 读取编码器速度反馈
5. 读取超声波和 MPU6050 数据
6. 根据状态选择：
   - 正常循迹 PID
   - 避障状态机
   - 丢线转向状态机
7. 输出左右电机 PWM
8. OLED 显示状态

但当前实际固件没有执行完整流程，仍处于调试版本状态。

## 7. 构建与开发说明

### 7.1 推荐开发环境

优先建议使用以下任一环境：

- `Keil MDK`
- `TI Code Composer Studio`

### 7.2 当前工程文件状态

- `keil/empty_LP_MSPM0G3507_nortos_keil.uvprojx`
  - 文件路径与当前目录结构基本匹配
- `gcc/makefile`
  - 仍引用 `../empty.c`
  - 但当前主入口文件实际位于 `App/empty.c`
  - 说明 GCC 构建脚本大概率未同步更新

### 7.3 SysConfig 注意事项

- 任何引脚修改都应优先通过 `empty.syscfg` 完成
- 修改后重新生成：
  - `ti_msp_dl_config.c`
  - `ti_msp_dl_config.h`
- 不建议手改自动生成文件

## 8. 已知问题

### 8.1 README 编码异常

仓库中的 `README.md` 当前存在乱码，内容大概率是原始中文说明，但文件编码与当前查看环境不一致。

### 8.2 主循环仍是调试态

当前主循环没有真正运行完整的小车控制逻辑，表现为：

- 固定 `Set_PWM(50, 50)`
- 持续发送串口调试数据

如果直接烧录到实车，可能会导致小车上电即按固定速度前进。

### 8.3 GCC 工程可能失效

`gcc/makefile` 中的入口文件路径未和当前目录结构同步，直接使用 GCC 工程前需要修正。

## 9. 建议下一步

若要把工程恢复到“可跑完整功能”的状态，建议按下面顺序推进：

1. 先确认电机、编码器、循迹、IMU、超声波的物理接线与本文件一致
2. 核对 `Bsp/Motor.c` 中 PWM 通道和左右轮映射关系
3. 恢复主循环中的：
   - 传感器读取
   - `Handle_Keys()`
   - `PID_control()`
   - `ObstacleAvoidance_Task()`
   - `turn_Task()`
4. 再进行单模块联调，而不是一次性全开

