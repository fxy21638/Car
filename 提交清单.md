# 提交清单

## 提交信息

- 分支：`past`
- 建议提交标题：`重构固件工程并完善安全故障保护`
- 目标远程：`origin/past`
- 整理日期：2026-07-11

## 本次变更

### 工程结构

- 将应用、控制、驱动和任务代码统一迁移到 `src/`。
- 将 Keil、GCC 和 SysConfig 文件整理到 `platform/`。
- 删除重复的 `Hardware`、旧 TIClang/IAR 工程和历史源码副本。
- 将 MSPM0 SDK 2.05.01.00 的必要头文件、设备定义及 Keil/GCC DriverLib 固化到 `third_party/`。
- Keil 和 GCC 改为引用同一套源码和工程内 SDK 相对路径。

### 安全与实时性

- 运行期间持续扫描 K1 急停，统一关闭 PWM 并复位控制状态。
- 控制环改为 TIMA0 驱动的固定 10 ms 事件调度。
- 修复超声波无回波永久忙碌问题，空闲时停止 20 kHz 定时器。
- 增加 IMU 初始化/运行失败、任务超时和编码器堵转停车。
- 增加电机零输出、换向保护、WWDT 和异常安全复位。
- 编码器累计值改为临界区一致快照。
- OLED 刷新限制为 100 ms，并修复格式化缓冲区风险。

### 测试与文档

- 提取可独立测试的 PID 核心与角度归一化模块。
- 增加 `tests/host` CMake 测试骨架。
- 更新 README、工程审查报告、构建方法和实车验收步骤。
- 记录 MSPM0G3507 工程名称与 LQFP-48 SysConfig 生成 MSPM0G3505 宏之间的历史差异。

## 已完成验证

- TI Arm Clang 全量编译和链接通过。
- 生成镜像约 21.4 KB，BSS 约 2.6 KB。
- Keil 工程 XML 及全部文件路径校验通过。
- 主机测试源码交叉编译语法检查通过。
- `git diff --check` 通过。

## 提交后待验证

- 在安装 Keil 的环境执行 Rebuild。
- 安装 Arm GNU Toolchain 后执行 GCC `clean all`。
- 核对实物 MCU 丝印，统一 MSPM0G3507/G3505 配置。
- 架空车轮测试 K1 急停、PWM 零输出和换向波形。
- 执行超声波、MPU6050、编码器断线及看门狗故障注入。
- 使用逻辑分析仪确认 10 ms 控制周期和小于 0.1 ms 的目标抖动。

